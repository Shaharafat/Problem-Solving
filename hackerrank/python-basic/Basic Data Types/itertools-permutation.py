def premutation(list,cp_list):
  temp = cp_list
  for i in cp_list:
  []
if __name__ == "__main__":
    S = input()

    text, count = S.split(" ")
    text = list(text)
    print(text, count)
    permutation(text,text)
